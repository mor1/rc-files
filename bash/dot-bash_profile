[ -r ~/.environment ] && source ~/.environment

uname -a
date
echo

if [[ $(tty) == /dev/tty1 ]]; then
  gpg --import F8827E91.asc
  keychain --clear
  eval $(keychain --eval --agents gpg,ssh bitbucket-key-rsa github-key cucl-key)
  exec sway
fi
